server.port=8085
spring.application.name=auth-service

spring.datasource.url=jdbc:mysql://localhost:3306/authdb
spring.datasource.username=root
spring.datasource.password=Yash@150802
spring.jpa.hibernate.ddl-auto=update

eureka.client.service-url.defaultZone=http://localhost:8761/eureka
# auth-service/src/main/resources/application.properties

# JWT Configuration (Crucial: Secret key MUST match API Gateway)
application.security.jwt.secret-key=YOUR_VERY_LONG_AND_SECURE_BASE64_ENCODED_SECRET_KEY_HERE_MIN_32_CHARS_FOR_HS256
application.security.jwt.expiration=86400000 

# ... other existing properties (e.g., database, server port, Eureka client)